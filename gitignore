# 생성: https://www.toptal.com/developers/gitignore/api/windows,intellij,java
# https://www.toptal.com/developers/gitignore?templates=windows,intellij,java에서 편집

# ## 인텔리즈 ###
# JetBrains IDE 포함: IntelliJ, RubyMine, PhpStorm, AppCode, PyCharm, CLion, Android Studio, WebStorm 및 Rider
# 참조: https://intellij-support.jetbrains.com/hc/en-us/articles/206544839

# 사용자별 항목
.idea / ** / 작업 공간 .xml
. 아이디어 / ** / task.xml
.idea / ** / usage.statistics.xml
.idea / ** / 사전
.idea / ** / 선반

# AWS 사용자별
. 아이디어 / ** / aws.xml

# 생성된 파일
.idea / ** / contentModel.xml

# 민감하거나 이탈률이 높은 파일
.아이디어 / ** / 데이터 소스 /
.idea / ** / dataSources.ids
.아이디어 / ** / dataSources.local.xml
. 아이디어 / ** / sqlDataSources.xml
.아이디어 / ** / dynamic.xml
. 아이디어 / ** / uiDesigner.xml
. 아이디어 / ** / dbnavigator.xml

# 그라들
. 아이디어 / ** / gradle.xml
.idea / ** / 라이브러리

# 자동 가져오기 기능이 있는 Gradle 및 Maven
# 자동 가져오기와 함께 Gradle 또는 Maven을 사용할 때 모듈 파일을 제외해야 하며,
# 다시 생성되어 변동을 일으킬 수 있기 때문입니다. 사용하는 경우 주석 해제
# 자동 가져오기.
# . 아이디어/인공물
# . 아이디어/컴파일러.xml
# .idea/jarRepositories.xml
# .idea/modules.xml
# . 아이디어/*.iml
# . 아이디어/모듈
# *.iml
# *.ipr

# 씨메이크
cmake-build- * /

# 몽고 익스플로러 플러그인
. 아이디어 / ** / mongoSettings.xml

# 파일 기반 프로젝트 형식
* .iws

# 인텔리제이
밖으로 /

# mpeltonen/sbt-idea 플러그인
.idea_modules /

# JIRA 플러그인
atlassian-ide-plugin.xml

# 필기체 클로저 플러그인
. 아이디어 / replstate.xml

# SonarLint 플러그인
.아이디어 / 소나린트 /

# Crashlytics 플러그인(Android Studio 및 IntelliJ용)
com_crashlytics_export_strings.xml
crashlytics.properties
crashlytics-build.properties
fabric.properties

# 에디터 기반 Rest 클라이언트
.idea / http요청

# Android 스튜디오 3.1 이상 직렬화된 캐시 파일
.idea / 캐시 / build_file_checksums.ser

# ## Intellij 패치 ###
# 댓글 이유: https://github.com/joeblau/gitignore.io/issues/186#issuecomment-215987721

# *.iml
# 모듈.xml
# . 아이디어/misc.xml
# *.ipr

# Sonarlint 플러그인
# https://plugins.jetbrains.com/plugin/7973-sonarlint
.idea / ** / 소나린트 /

# SonarQube 플러그인
# https://plugins.jetbrains.com/plugin/7238-sonarqube-community-plugin
.idea / ** / sonarIssues.xml

# 마크다운 네비게이터 플러그인
# https://plugins.jetbrains.com/plugin/7896-markdown-navigator-enhanced
.idea / ** / markdown-navigator.xml
.idea / ** / markdown-navigator-enh.xml
.idea / ** / 마크다운 내비게이터 /

# 캐시 파일 생성 버그
# https://youtrack.jetbrains.com/issue/JBR-2257 참조
.아이디어 / $ CACHE_FILE $

# CodeStream 플러그인
# https://plugins.jetbrains.com/plugin/12206-codestream
. 아이디어 / codestream.xml

# IntelliJ 플러그인용 Azure 도구 키트
# https://plugins.jetbrains.com/plugin/8053-azure-toolkit-for-intellij
. 아이디어 / ** / azureSettings.xml

# ## 자바 ###
# 컴파일된 클래스 파일
* .클래스

# 로그 파일
* .로그

# BlueJ 파일
* .ctxt

# 자바용 모바일 도구(J2ME)
.mtj.tmp /

# 패키지 파일 #
* .jar
* .war
* .나르
* .귀
* .zip
* .tar.gz
* .rar

# 가상 머신 충돌 로그, http://www.java.com/en/download/help/error_hotspot.xml 참조
hs_err_pid *
replay_pid *

# ## 윈도우 ###
# Windows 썸네일 캐시 파일
Thumbs.db
Thumbs.db:암호화 가능
ehthumbs.db
ehthumbs_vista.db

# 덤프 파일
* .스택 덤프

# 폴더 설정 파일
[ Dd ] esktop.ini

# 파일 공유에 사용되는 휴지통
$RECYCLE.BIN /

# 윈도우 인스톨러 파일
* .택시
* .msi
* .msix
* .msm
* .msp

# 윈도우 단축키
* .lnk

# ## 그래들 ###
.gradle
** / 빌드 /
! src / ** / 빌드 /

# Gradle GUI 구성 무시
gradle-app.setting

# Gradle 래퍼 jar 파일을 무시하지 마십시오(.jar 파일은 일반적으로 무시됨).
! gradle-wrapper.jar

# Gradle 래퍼 속성을 무시하지 마십시오 .
! gradle-wrapper.properties

# 프로젝트 캐시
.gradletasknamecache

# Eclipse Gradle 플러그인 생성 파일
# 이클립스 코어
.프로젝트
# JDT 전용(Eclipse Java 개발 도구)
.classpath

# https://www.toptal.com/developers/gitignore/api/gradle 끝